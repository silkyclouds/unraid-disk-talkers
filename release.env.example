# Copy this file to `release.env` and fill in the real public values before
# running `./build-release.sh`.

GITHUB_REPO="silkyclouds/unraid-disk-talkers"
GITHUB_BRANCH="main"
SUPPORT_URL="https://forums.unraid.net/topic/198305-plugin-disk-talkers/"

# Optional. Leave empty to reuse the version from ./VERSION.
RELEASE_TAG=""

# Optional.
PLUGIN_AUTHOR="meaning"
WRITE_ROOT_MANIFEST="0"
OUTPUT_DIR=""
